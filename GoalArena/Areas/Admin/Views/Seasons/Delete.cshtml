@model GoalArena.Models.Season

@{
    ViewData["Title"] = "Delete Season";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container p-3">
    <h2 class="text-danger">Delete Season</h2>
    <hr />
    <h3>Are you sure you want to delete this season?</h3>
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Name)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Name)</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.StartDate)</dt>
        <dd class="col-sm-10">@Model.StartDate.ToShortDateString()</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.EndDate)</dt>
        <dd class="col-sm-10">@Model.EndDate.ToShortDateString()</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.TournamentId)</dt>
        <dd class="col-sm-10">@Model.TournamentId</dd>
        <dt class="col-sm-2">Tournament</dt>
        <dd class="col-sm-10">@((Model.Tournament != null) ? Model.Tournament.Name : "N/A")</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Status)</dt>
        <dd class="col-sm-10">@Model.Status</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="SeasonId" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
